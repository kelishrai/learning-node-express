REPL - read-eval-print loop
CLI
https://github.com/john-smilga/node-express-course
GLOBAL
__dirname - path to current directory
__filename - file name
require - function to use modules (CommonJS)
module - info about current module (file)
process - info about env where the program is being executed
MODULES - smaller files, more structure, share minimum
module.exports = {};
require('./fileName');
https://nodejs.org/dist/latest-v20.x/docs/api/
OS MODULE - operating with os/server
os.userInfo();os.uptime();os.type();os.release();os.totalmem();os.freemem();
Path MODULE - provides utilities to work with file and directory paths
path.sep; path.join(); path.basename(); path.resolve();
FS MODULE - 
const readFile = fs.readFileSync('file_location', 'charset');
const writeFile = fs.writeFileSync('file_location', input_to_be_written, {flag: 'a'});
Asynchronous fs
const { readFile } = require('fs');
readFile('file_location','charset',(err, result)=>{}){
    const myString = result;
    writeFile('file_location',myString,(err, result)=>{})
};
HTTP MODULE -
const server = http.createServer((req, res)=>{
    if(req.url === '/'){
        res.end("Welcome to our homepage!");
    }
})
server.listen(5000);
package, dependency, module mean the same in js
npm -
npm init
npm init -y
npm i <packageName>
npm install -g <packageName>
npm i lodash
npm i bootstrap
const _ = require('lodash');
const myArr = [1,[2,[3,[4,[5]]]]];
const myNewArr = _.flattenDeep(myArr);